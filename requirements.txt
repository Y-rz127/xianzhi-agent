# AI 框架
# 注意：langchain-dashscope 0.1.8 不支持 bind_tools(Function Calling)，
# 故改用 langchain-openai 通过 OpenAI 兼容模式调用千问
langchain-core>=1.0.0,<2.0.0
langchain-openai>=1.0.0,<2.0.0
langchain-community>=0.4.0,<0.5.0
langgraph>=1.0.0,<2.0.0

# Web 框架
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.32.0
sse-starlette>=2.1.0

# 配置与校验
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.1

# 八字排盘
lunar-python>=1.3.4

# HTTP / 抓取
httpx>=0.27.0
beautifulsoup4>=4.12.0

# 日志
loguru>=0.7.2

# MCP 协议（高德地图 MCP server 接入）
mcp>=1.28.0

# PostgreSQL 记忆 / pgvector
psycopg[binary]>=3.2.0
langchain-postgres>=0.0.17,<0.1.0

# PDF 报告
reportlab>=4.0.0

# 向量库（RAG）
chromadb>=1.0.0,<2.0.0
langchain-chroma>=1.0.0,<2.0.0
langchain-text-splitters>=0.2.0
dashscope>=1.20.0
