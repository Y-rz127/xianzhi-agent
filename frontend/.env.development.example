# Web 端本地开发：直连本地后端（与小程序同款方案，web 端不单独部署）
# 用法：复制本文件为 .env.local（已被 gitignore，不会提交），并把地址换成你的本地后端地址
#
#   cp .env.development.example .env.local
#
# 本地后端默认地址（main.py 监听 8123，docker-compose 亦映射 8123:80）
# 例：VITE_API_BASE=http://localhost:8123/api
VITE_API_BASE=http://localhost:8123/api

# 管理端 API Key（可选）：仅当后端配置了 API_KEYS 时需要，取值须在后端 API_KEYS 列表内。
# 本地开发后端 API_KEYS 默认为空（鉴权关闭），可不配置。
# VITE_API_KEY=your-admin-api-key

