# 先知智能体持续学习优化清单

## 阶段一：结构化案例检索增强

- [x] 建立独立案例检索模块，不再把命例只当普通知识片段使用。
- [x] 从 `27_命例案例库.md` 自动拆分种子命例。
- [x] 支持 `data/cases/*.json` 结构化案例扩展。
- [x] 按领域、日主五行、强弱、文本相关度混合排序。
- [x] 在 Worker prompt 中注入相似命例参考。
- [x] 明确提示模型只参考断法路径，不照搬案例结论。
- [x] 将高质量真实反馈自动沉淀为结构化案例。
- [ ] 增加案例命中率、命中案例 ID 的观测指标。

## 阶段二：反馈闭环与偏好数据

- [x] 新增回答级 `up/down` 反馈数据表。
- [x] 新增回答反馈提交接口。
- [x] 新增回答反馈列表接口，便于后台筛选训练样本。
- [x] PC 端聊天回答下方增加"有帮助 / 不太准"反馈按钮。
- [x] 保存会话、问题、回答、命盘快照和反馈方向。
- [x] 增加差评原因标签。
- [x] 在后台增加反馈统计看板。
- [x] 将好评样本人工审核后转入案例库。

## 阶段三：LoRA 微调准备

- [x] 定义训练样本格式：system、input、output、chart_snapshot、domain。
- [x] 从好评反馈导出 SFT 训练集。
- [x] 增加人工审核/修订字段，避免把错误回答喂回模型。
- [ ] 先用 100-200 条样本做小规模 LoRA 验证。
- [ ] 对比微调前后在固定评测集上的表现。

## 阶段四：DPO 与持续学习流水线

- [x] 从同类问题构造 chosen/rejected 偏好对。
- [x] 增加 DPO 数据导出接口。
- [ ] 每 200-500 条有效反馈触发一次离线训练候选。
- [ ] 用 A/B 测试决定是否上线新模型。
- [ ] 建立回滚机制，避免坏样本导致模型口径退化。